import 'package:flutter/material.dart';

class AppColors {
  // Train statuses
  static const onTime = Color(0xFF22C55E);
  static const delayed = Color(0xFFF59E0B);
  static const cancelled = Color(0xFFEF4444);
  static const offline = Color(0xFF60A5FA);
  
  // Generic
  static const secondary = Color(0xFF9CA3AF);

  // Light Theme
  static const bgLight = Color(0xFFFFFFFF);
  static const textLight = Color(0xFF111827);

  // Dark Theme
  static const bgDark = Color(0xFF1F2937);
  static const textDark = Color(0xFFF9FAFB);
}
